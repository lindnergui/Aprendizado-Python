{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Rodar como Módulo",
            "type": "debugpy",
            "request": "launch",
            "program": "${file}",
            "cwd": "${workspaceFolder}",
            "env": { "PYTHONPATH": "${workspaceFolder}" },
            "console": "integratedTerminal"
        }
    ]
}